<!--
  BEAT SHEET SOURCE FILE
  Replace everything below with the real beat sheet. The /story/beat-sheet
  page reads this file directly - no code changes needed.

  Formatting the parser understands:
    - A line starting with "## " is a section heading, e.g. "## Act One".
    - A line starting with "- " is a list item under the current section.
      Keep each item on a SINGLE line, however long - the parser doesn't
      join wrapped lines back into one item.
    - Plain lines (no "- ") are read as a paragraph.
    - Separate sections/paragraphs with a BLANK LINE; list items can be
      stacked directly under each other with no blank line between them.
-->

## Act One

- Beat sheet coming soon.
- Drop the real beats into this file, one "- " item per beat, grouped under "## " section headings.
