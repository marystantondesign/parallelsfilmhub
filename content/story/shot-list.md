<!--
  SHOT LIST SOURCE FILE
  Replace everything below with the real shot list. The /story/shot-list
  page reads this file directly - no code changes needed.

  Formatting: a standard Markdown table - a header row, a separator row of
  dashes, then one data row per shot. Pipes at the very start/end of a row
  are optional.
-->

| Shot | Scene | Description | Notes |
| --- | --- | --- | --- |
| 1 | - | Shot list coming soon. | Drop the real shots into this table. |
